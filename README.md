# youtube_metrics_analyzer
Приложение читает файлы с данными о видео (см. пример ниже) и формирует отчеты

## Установка

```bash
# Установка проекта в режиме разработки
pip install -e .

# Или только зависимостей
pip install tabulate>=0.9.0
